<%= link_to "Home", root_path, :class => 'brand' %>
<ul class="nav">
    <% if current_user %>
        <li>
            <%= link_to "My Account", account_path, :class=>'brand' %>
        </li>
        <li>
            <%= link_to "Logout", logout_path , :class=>'brand', :confirm=>"You sure?" %>
        </li>
    <% else %>
        <li>
            <%= link_to "Register", new_account_path , :class=>'brand' %>
        </li>
        <li>
            <%= link_to "Login", login_path , :class=> 'brand' %>
        </li>
    <% end %>
</ul>
